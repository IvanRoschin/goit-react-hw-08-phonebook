import * as React from 'react';

import Button from '@mui/material/Button';

const Verify = () => {
  return (
    <div>
      <Button variant="contained">Recend verification email</Button>
    </div>
  );
};

export default Verify;
